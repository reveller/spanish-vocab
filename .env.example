# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Only needed on first run to seed the initial user. Remove after seeding.
SEED_USER_EMAIL=
SEED_USER_PASSWORD=
